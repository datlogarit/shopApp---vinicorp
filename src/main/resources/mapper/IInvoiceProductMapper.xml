<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- namespace define the path of interfaceMapper -->
<mapper namespace="com.example.shop_app.mapper.IInvoiceProductMapper">
    <!-- create a new invoice -->
    <insert id="createInvoiceProduct">
        insert into invoice_product(invoice_id, product_id, quantity)
        values (#{invoiceProduct.invoiceId}, #{invoiceProduct.productId}, #{invoiceProduct.quantity})
    </insert>
</mapper>